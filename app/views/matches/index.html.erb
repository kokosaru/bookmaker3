<% @matches.each do |match| %>
	<%= match.genre %>
	<% match.teams.each do |team| %>
		<%= team.team_name %>
	<% end %>
  	<%= match.match_name %>
  	<%= match.match_day.strftime("%m月 %d日 %H:%M") %>
  <br>
<% end %>
